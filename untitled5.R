lcm_vector <- function(x) Reduce(lcm, x)
lcm_vector(c(20,50,75))

library(schoolmath)
prime.factor(1001)
gcd(1280,17)
a = scm(3,5)
b = scm(7,9)
scm(a,b)

perm = function(n, x) {
      factorial(n) / factorial(n-x)
    }
    
    comb = function(n, x) {
      factorial(n) / factorial(n-x) / factorial(x)
    }
    
    perm = function(24, 3) {
      factorial(24) / factorial(3)
    }
    
    comb = function(24, 3) {
      factorial() / factorial(7)) / factorial(9)
    }
    
    permn(24)
    combn(5, 3)
    length(permn(24))
    dim(combn(5,3))[2]
    
    
    factorial(5)

    
